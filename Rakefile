desc "Open an irb session preloaded with this library"
task :server do
  sh "ruby -rubygems ./remember-me.rb"
end

desc "Open an irb session preloaded with this library"
task :console do
  sh "irb -rubygems -I lib -r ./remember-me.rb"
end